# Djehuti Router Install Notes

This folder will carry the install metadata for the first virtual audio cable package.

The first package should expose:

- `Djehuti Router Monitor`
- `Djehuti Router Capture`


# Endpoint Map

This is the first audio shape we want the driver to expose.

## First cable

- render endpoint: `Djehuti Router Monitor`
- capture endpoint: `Djehuti Router Capture`

## Later endpoints

- `Djehuti Router Mic`
- `Djehuti Router Instrument`

## Shared format target

- 48 kHz
- 2 channels
- keep names stable so the app can map to them reliably

